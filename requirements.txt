# Runtime dependencies (compatible-release pins reduce surprise breakage)
gymnasium~=1.3.0
numpy~=2.2.0
PyYAML~=6.0.0

# Dev/test dependencies (also listed in pyproject.toml optional-dependencies)
pytest~=8.0.0
